Code marker scan (TODO|FIXME|HACK|XXX) over src/
Date: 2026-04-26

Result: 0 matches.

Verified via Grep tool with pattern "TODO|FIXME|HACK|XXX" path src/
(ripgrep, all output modes returned no matches).

For broader context, here are matches under tests/ and experiments/ (informational only, not part of src/ scope):

tests/ matches:
  test_embedding_phylo.py:841: "|orgXXX" — placeholder string in docstring (not a marker)

experiments/ matches:
  41_768d_retention_benchmarks.py:264 — "XXX" in comment as placeholder for ID (not a marker)
  35_embedding_phylogenetics.py:1982 — "|orgXXX" — placeholder string in docstring (not a marker)
  50_sequence_to_oe.py:439 — REAL TODO: 'print("\\n[TODO] Downstream evaluation (SS3, disorder, retrieval)")'
    (Exp 50 is the active seq2oe / Stage 3 work; this is a known follow-up.)

Net: zero true markers in src/, one true TODO in experiments/ (Exp 50, expected, queued).
